export interface Achievement {
  title: string;
  description: string;
}

export interface Service {
  title: string;
  description: string;
}

export interface TrainingPillar {
  title: string;
  description: string;
}
